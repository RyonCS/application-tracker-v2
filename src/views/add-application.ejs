<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/styles/editandAddApplication.css">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet"/>
    <title>Add a new application!</title>
</head>
<body>
  <!-- Header -->
  <header class="p-3 text-bg-dark">
    <div class="container-fluid">
        <div class="d-flex align-items-center justify-content-between">
        
            <!-- Back to Jobs Button -->
            <form action="/applications/my-applications" method="get" class="m-0">
                <button class="btn btn-outline-light">Back to Jobs</button>
            </form>

            <!-- Centered Title -->
            <h3 class="text-white m-0 text-center flex-grow-1">Job Application Tracker</h3>

            <!-- Log Out Button -->
            <form action="/auth/logout" method="post" class="m-0">
                <button class="btn btn-outline-light">Log Out</button>
            </form>

            </div>
        </div>
    </header>

    <form action="/applications/my-applications" method="POST">
    <table id="editApplicationsTable">
        <thead style="background-color:#C8C8C8 !important;">
            <tr>
                <th>Date</th>
                <th>Company</th>
                <th>Position</th>
                <th>Location</th>
                <th>Work Mode</th>
                <th>Status</th>
                <th>Link to Application</th>
                <th>Action</th>
            </tr>
        </thead>
        
        <tbody>
        <tr>
            <td>
            <% const formattedDate = date ? date.toISOString().split('T')[0] : '' %>
            <input type="date" name="applicationDate" value="<%=formattedDate %>" class="form-control mx-auto"/>
            </td>
            <td><input type="text" name="company" placeholder="Company" class="form-control mx-auto"/></td>
            <td><input type="text" name="position" placeholder="Position" class="form-control mx-auto"/></td>
            <td><input type="text" name="location" placeholder="Location" class="form-control mx-auto"/></td>
            <td>
            <select name="workMode">
                <option value="In-Person">In-Person</option>
                <option value="Remote">Remote</option>
                <option value="Hybrid">Hybrid</option>
            </select>
            </td>
            <td>
            <select name="status">
                <option value="Applied">Applied</option>
                <option value="Interview">Interview</option>
                <option value="Offer">Offer</option>
                <option value="Rejected">Rejected</option>
            </select>
            </td>
            <td><input type="text" name="linkToJobPosting" placeholder="Link" class="form-control mx-auto"/></td>
            <td class="text-center">
                <button type="submit" class="btn btn-primary btn-sm rounded-pill px-3" style="font-size: 0.85rem;">Submit</button>
            </td>
        </tr>
        </tbody>
    </table>

    
    </form>

    <!-- 100% privacy-first analytics -->
    <script async src="https://scripts.simpleanalyticscdn.com/latest.js"></script>

</body>
</html>